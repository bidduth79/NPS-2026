with open('app/src/main/java/com/example/ui/components/CustomBottomNavigation.kt', 'r') as f:
    content = f.read()

import re
# Check if magnetic effect is already there or how we can add scroll listener
